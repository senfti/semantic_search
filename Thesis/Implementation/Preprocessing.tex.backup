\section{Preprocessing}

The images and the point clouds can be used without preprocessing. The laser scans on the other hand need have to be modified for further use. The scan{\_}filter node prepares the laser scans for the mapping. Therefore range measurements on the far right and far left of the field of view are discarded. This is necessary as some parts of the robot are visible to the LIDAR. Further, invalid range measurements have to be considered. There are two reasons for invalid range measurements. Firstly, the laser beam was not sufficiently reflected. In this case the range measurement should be ignored. Secondly, no obstacle is within the range of the LIDAR. In this case the range measurement should be set to the maximum range, so the mapping can use those beams to clear space. Due to the short range of the used LIDAR this happens a lot. There is no way to distinguish those two cases and always assuming one case is not sufficient. Discarding all invalid measurements causes problems in open areas and corridors, setting to maximum range clears to much space. Therefore only invalid measurements of every fifth scan are set to maximum range, which tests showed to be a reasonable trade-off.

The navigation is done in 2D, but also the obstacles only seen by the RGBD-camera have to be taken into account. Therefore the laser scan sent to the local planner is modified to contain also those obstacles. This is done by intersecting the beams sent by the LIDAR with the projected point cloud of the RGBD-camera. The range measurement of a beam is then the minimum of the range measurement from the LIDAR and the distance to the first intersection with the projected point cloud. This is show in figure \ref{laser}. 

\begin{figure}
  \includegraphics[width=0.9\textwidth,clip]{figures/laser.png}
  \caption[Laser scan preprocessing]{Preprocessing of the laser scan: Discarded 
  
  Green points are discarded by the scan{\_}filter node; red points are the result of the scan{\_}filter node; black dots are the projected point cloud of the RGBD-camera; blue points are the points inserted by the scan{\_}costmap node}
  \label{laser}
\end{figure}